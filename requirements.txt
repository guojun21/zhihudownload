# 知乎视频下载器依赖

# HTTP 请求
requests>=2.28.0

# 从 Chrome 读取 cookies (需要 macOS Keychain 授权)
browser-cookie3>=0.19.1

# M3U8 解析
m3u8>=3.6.0

# FastAPI 后端服务
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Whisper 语音转文字
openai-whisper>=20231117

# 可选：如果 browser-cookie3 有问题，可以使用 pycookiecheat
# pycookiecheat>=0.5.0

# 系统依赖 (需要通过 brew 安装):
# ffmpeg - 用于下载和合并 M3U8 视频流，以及提取音频
# 安装命令: brew install ffmpeg
