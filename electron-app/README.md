# 知乎视频下载器 - Electron 前端

基于 Electron + Vite + React + TypeScript 构建的知乎视频下载器桌面应用。

## 技术栈

- **Electron** - 桌面应用框架
- **Vite** - 构建工具
- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Lucide React** - 图标库

## 开发

### 前置要求

1. Node.js 18+
2. Python 3.8+ (用于后端服务)
3. FFmpeg (用于视频下载)

### 安装依赖

```bash
# 安装前端依赖
npm install

# 安装后端依赖
cd ..
pip install -r requirements.txt
```

### 开发模式

```bash
# 启动前端开发服务器
npm run dev

# 在另一个终端启动 Electron (会自动启动 Python 后端)
npm run dev:electron
```

### 构建

```bash
# 构建应用
npm run build

# 打包为可分发格式
npm run dist
```

## 项目结构

```
electron-app/
├── electron/           # Electron 主进程代码
│   ├── main.ts        # 主进程入口
│   └── preload.ts     # 预加载脚本
├── src/               # React 前端代码
│   ├── api/           # API 通信层
│   ├── types/         # TypeScript 类型定义
│   ├── App.tsx        # 主组件
│   ├── App.css        # 样式
│   └── main.tsx       # 入口文件
├── index.html         # HTML 模板
├── package.json       # 项目配置
└── vite.config.ts     # Vite 配置
```

## 功能

- 📥 解析知乎训练营视频 URL
- 🎬 支持多种清晰度选择
- 📁 自定义下载目录
- 📊 实时下载进度显示
- 🔐 自动读取 Chrome cookies 进行认证

